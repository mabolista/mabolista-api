const errorCode = require('./errorCode');
const errorStatusCode = require('./errorStatusCode');
const cloudinaryMediaFolderPath = require('./cloudinaryMedia');

module.exports = {
  cloudinaryMediaFolderPath,
  errorCode,
  errorStatusCode
};
